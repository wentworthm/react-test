sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t wentworthm/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true wentworthm/docker-react npm run test
